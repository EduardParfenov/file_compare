# Задачи: одинаковое отображение всех строк таблицы

## 1. Рендер (spec: diff-viewer)

- [x] 1.1 В `app/static/app.js` рендерить все строки таблицы как `<td>`
  (убрать `<th>` для первой строки)
- [x] 1.2 В `app/static/style.css` удалить стили `.diff-table th`
  (жирный шрифт, серая заливка)
- [x] 1.3 Проверить рендер в headless Chrome: в таблице нет `<th>`,
  все строки одинаковые

## 2. Финализация

- [x] 2.1 Прогнать `pytest` (74 теста зелёные)
- [x] 2.2 Обновить основную спеку `openspec/specs/diff-viewer/spec.md`
- [ ] 2.3 Проверка пользователем, затем архивация change и коммит

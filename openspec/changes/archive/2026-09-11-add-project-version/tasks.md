# Tasks: add-project-version

- [x] 1. Создать `VERSION` в корне репозитория с содержимым `0.1.0`.
- [x] 2. В `app/__init__.py` читать `VERSION` при старте в
  `app.config["APP_VERSION"]`; при отсутствии/нечитаемости файла —
  запасное значение `0.0.0-dev`, без падения.
- [x] 3. В `app/routes.py` передать версию в `index.html`; в шаблоне
  добавить подвал с версией; при необходимости минимальный стиль в
  `app/static/style.css`.
- [x] 4. Тесты: версия из `VERSION` попадает в конфиг; отсутствие файла →
  запасное значение; HTML главной страницы содержит версию.
- [x] 5. Обновить `AGENTS.md`: файл `VERSION` в структуре проекта и уточнение
  правила бампа (куда повышается версия при архивации).
- [x] 6. Прогнать `ruff format --check .`, `ruff check .`, `pytest`.
- [x] 7. `openspec validate add-project-version --strict` — без ошибок.

# Задачи: опрос статуса задачи каждую секунду

## 1. Изменение интервала опроса

- [x] 1.1 В `app/static/app.js` изменить `POLL_INTERVAL_MS` с `3000` на `1000`; проверить, что логика опроса не тронута: первый запрос немедленный (`tick()` до `setInterval`), степпер и ветки done/failed без изменений (`git diff app/static/app.js` показывает только константу)
- [x] 1.2 Обновить комментарий к интервалу при необходимости, чтобы он соответствовал значению 1000 мс; проверить, что в файле не осталось упоминаний «3 секунды»/3000

## 2. Документация

- [x] 2.1 В `REVIEW.md` (раздел 1, пункт «comparison-jobs / Визуализация процесса обработки») заменить «далее — каждые 3 секунды» на «далее — каждую 1 секунду»; проверить, что пункт соответствует действующей спеке и по-прежнему отмечает отсутствие тестового покрытия клиентской части

## 3. Проверка

- [x] 3.1 Прогнать `pytest` — все тесты проходят (изменение клиентское, регрессий серверной части быть не должно)
- [x] 3.2 Проверить страницу в браузере (или headless Chrome): во время сравнения запросы `GET /api/jobs/<job_id>` идут с интервалом ~1 с после немедленного первого (видно в DevTools/Network); поведение степпера при успехе и ошибке — как раньше

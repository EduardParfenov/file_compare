# Tasks: fix-diff-colors-and-table-fallback

## 1. Тесты (TDD — сначала тесты)

- [x] 1.1 Добавить в `tests/test_jobs.py` тест fallback таблиц: monkeypatch недоступности пословного diff (см. design.md D3), изменённая строка таблицы → `cell_segments` изменённой ячейки равны `[{"text": <старое>, "type": "del"}]` (left) и `[{"text": <новое>, "type": "add"}]` (right), без сегментов типа `same` в изменённой ячейке; проверить, что тест падает на текущем коде
- [x] 1.2 Добавить тест-фиксацию двухцветной модели: результат задачи с фрагментами changed/removed/added — все типы сегментов в `segments` и `cell_segments` принадлежат `{same, del, add, del-mark, add-mark}`, значения `change` принадлежат `{changed, removed, added, None}`; проверить, что тест проходит на текущем коде
- [x] 1.3 Исправить комментарий `tests/test_jobs.py:250`: убрать «жёлтый — добавлен», описать двухцветную модель (красный — удалённое/старая сторона, зелёный — добавленное/новая сторона); проверить, что текст комментария соответствует спеке diff-viewer

## 2. Код

- [x] 2.1 Исправить fallback табличной ветки в `app/services/jobs.py` (`_build_rows`): при `inline_diff(a, b) is None` подставлять `([{"text": a, "type": "del"}], [{"text": b, "type": "add"}])` (пустой текст — пустой список) вместо `_plain_segments`; проверить, что тест 1.1 проходит
- [x] 2.2 Удалить `_plain_segments`, если других вызовов нет (проверить grep по `app/`); проверить `pytest` без новых падений
- [x] 2.3 Исправить комментарий `app/static/app.js:191`: убрать «изменено — жёлтый», описать двухцветную модель (добавлено — зелёный, удалено — красный); проверить, что комментарий соответствует спеке diff-viewer
- [x] 2.4 Проверить `app/static/style.css` и `app/static/app.js` на отсутствие жёлтого (`--yellow`, `--yellow-light`) в классах подсветки различий (`seg-*`, `change-*`, `placeholder-*`); при обнаружении — заменить на красный/зелёный по панелям; верификация: grep не находит жёлтых значений в селекторах подсветки различий

## 3. Проверка

- [x] 3.1 Прогнать `pytest` полностью — все тесты проходят
- [x] 3.2 Прогнать `openspec validate fix-diff-colors-and-table-fallback` — без ошибок
- [x] 3.3 Актуализировать `REVIEW.md`: убрать/исправить пункты о fallback ячеек таблиц (раздел 4) и вводящих комментариях (раздел 2); проверить, что формулировки соответствуют новому поведению

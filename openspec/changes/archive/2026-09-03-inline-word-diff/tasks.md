# Задачи: пословная подсветка внутри изменённых блоков

Change оформлен задним числом — все задачи уже выполнены, `pytest` зелёный
(62 теста).

## 1. Пословный diff (spec: document-diff, реализация)

- [x] 1.1 Написать тесты `tests/test_diffing.py::TestInlineDiff`: добавленное
  слово подсвечивается только на новой стороне; заменённое слово — на обеих;
  конкатенация сегментов воспроизводит исходный текст; одинаковые тексты без
  diff-сегментов
- [x] 1.2 Реализовать `inline_diff` в `app/services/diffing.py` (токены
  `\w+|\W+`, `SequenceMatcher`, слияние соседних сегментов, защита
  `MAX_INLINE_TOKENS`); убедиться, что тесты 1.1 зелёные

## 2. API результата (spec: comparison-jobs, реализация)

- [x] 2.1 Прикреплять `segments` к changed-парам в `_build_rows`
  (`app/services/jobs.py`)
- [x] 2.2 Обновить e2e-ожидания строк результата в `tests/test_jobs.py`
  (changed-пары содержат `segments`; removed/added — без `segments`)

## 3. Diff Viewer (spec: diff-viewer)

- [x] 3.1 Реализовать рендер сегментов в `app/static/app.js`: блок с
  `segments` не получает фоновый класс `change-*`, сегменты оборачиваются
  в `span` с классом по типу (`seg-add` / `seg-del` / `seg-chg`)
- [x] 3.2 Добавить стили `.seg-add` / `.seg-del` / `.seg-chg` в
  `app/static/style.css` (git-стиль: зелёный/красный/жёлтый)
- [x] 3.3 Проверить рендеринг в headless Chrome: добавленное слово —
  зелёным справа, удалённое — красным слева, изменённое — жёлтым с обеих
  сторон, блок без фоновой заливки

## 4. Финализация

- [x] 4.1 Прогнать `pytest` целиком (62 теста зелёные)
- [x] 4.2 Обновить основную спеку `openspec/specs/diff-viewer/spec.md`
  и README.md

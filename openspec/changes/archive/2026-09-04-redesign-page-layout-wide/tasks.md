# Tasks: redesign-page-layout-wide

## 1. Шапка страницы

- [x] 1.1 В `app/templates/index.html` обернуть `h1` и `p` в `<header class="page-header">` и убрать точку в конце подписи; проверить, что страница отдаётся Flask без ошибок (`pytest` проходит)
- [x] 1.2 В `app/static/style.css` добавить стиль `.page-header { text-align: center; }` (с сохранением существующих отступов `h1`/`p`); проверить визуально, что заголовок и подпись по центру

## 2. Адаптивная ширина страницы

- [x] 2.1 В `app/static/style.css` заменить `max-width: 1200px` на `max-width: 1920px` для `body` (сохранить `margin: 0 auto` и `padding: 24px`); проверить визуально, что при ширине окна >1200px страница растягивается, а при >1920px — центрируется
- [x] 2.2 Проверить визуально, что широкая таблица в diff viewer умещается в панели при ширине окна 1920px, а при переполнении по-прежнему работает горизонтальная прокрутка `.diff-table`

## 3. Центрированная колонка контролов и ширина 90%

- [x] 3.1 В `app/templates/index.html` обернуть `.upload-zones`, `.actions`, `.stepper`, `#error` и `#degraded` в `<div class="controls">`; проверить, что страница отдаётся Flask со статусом 200 и контейнер присутствует в разметке
- [x] 3.2 В `app/static/style.css` дополнить `body` правилом `width: 90%` (потолок `max-width: 1920px` сохранить); проверить, что при ширине окна <2134px страница занимает 90% окна
- [x] 3.3 В `app/static/style.css` добавить `.controls { max-width: 1150px; margin: 0 auto; }`, центрировать `.actions` (`text-align: center`) и `.stepper` (`justify-content: center`); проверить визуально, что зоны загрузки, кнопка и степпер по центру, а diff-панели на всю ширину

## 4. Регрессионная проверка

- [x] 4.1 Прогнать `pytest` и убедиться, что все тесты проходят (после групп 1-2)
- [x] 4.2 Повторно прогнать `pytest` после группы 3 и убедиться, что все тесты проходят

# Задачи: создание AGENTS.md

## 1. Создание AGENTS.md

- [x] 1.1 Создать `AGENTS.md` в корне проекта со следующими разделами: назначение и стек (Python 3.11, Flask, langchain-openai, pytest); структура каталогов (`app/`, `tests/`, `openspec/`, `uploads/`); установка и запуск (venv, `pip install -r requirements.txt`, `flask run`); тесты (`pytest`, LLM мокается, сеть не используется); конфигурация только через env (`.env.example`, без хардкода секретов и абсолютных путей); конвенции (пути из конфигурации, загрузки только в `UPLOAD_DIR`, `ALLOWED_EXTENSIONS`, английские идентификаторы + допустимы русские docstring/тексты UI, `pytest` перед коммитом); правила безопасности (`.env` и секреты не коммитятся, `MAX_CONTENT_LENGTH`); workflow OpenSpec (новая функциональность — через change-предложения в `openspec/changes/`, proposal содержит строку «уровень: несовместимое поведение» или «уровень: исправление»); запрет читать файлы каталога `input/`; `uploads/` не коммитится; ссылки на `README.md`, `REVIEW.md`, `openspec/`. Проверить: файл существует, все перечисленные разделы присутствуют, факты совпадают с `README.md` и `openspec/config.yaml` (без противоречий, без дублирования простыней — кратко и со ссылками)
- [x] 1.2 Сверить факты в `AGENTS.md` с проектом: команды запуска/тестов рабочие, переменные окружения совпадают с `.env.example`, структура каталогов актуальна. Проверить визуально по `README.md` и `.env.example`

- [x] 1.3 В `README.md` актуализировать описание опроса статуса (строка 28): «раз в 3 секунды» → «каждую 1 секунду» (устарело после архивированного change `poll-status-every-second`); проверить, что формулировка совпадает со спекой comparison-jobs

## 2. Проверка

- [x] 2.1 Прогнать `openspec validate add-agents-md` — change валиден (skip_specs, ошибок нет)
- [x] 2.2 Прогнать `pytest` — все тесты проходят (изменение только документации)

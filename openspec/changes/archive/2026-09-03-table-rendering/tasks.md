# Задачи: отображение таблиц как HTML-таблиц

Change оформлен задним числом — все задачи уже выполнены, `pytest` зелёный
(74 теста).

## 1. Структура таблиц в API (spec: diff-viewer, реализация)

- [x] 1.1 Написать тесты `tests/test_diffing.py`: `parse_table_row`
  (базовая строка, экранированный `\|` не разрезает ячейку, строка без
  окантовочных пайпов), `is_table_separator` (сепаратор/данные/текст)
- [x] 1.2 Реализовать `parse_table_row` (state-machine с обработкой `\|`),
  `is_table_separator`, `is_table_row` в `app/services/diffing.py`;
  убедиться, что тесты 1.1 зелёные
- [x] 1.3 Обогащать блоки результата в `app/services/jobs.py`: `cells` для
  строк данных, `sep` для служебной строки-разделителя; для changed-пар —
  `cell_segments` (пословный diff по ячейкам, `zip_longest` для разного
  числа колонок); e2e-тесты в `tests/test_jobs.py`

## 2. Рендер таблиц (spec: diff-viewer)

- [x] 2.1 Реализовать в `app/static/app.js` группировку подряд идущих строк
  таблицы в `<table>` (первая строка — `<th>`, `sep` пропускается,
  добавленные/удалённые строки — цветной `<tr>`, изменённые — пословно
  в ячейках, недостающие колонки — пустые ячейки)
- [x] 2.2 Добавить стили таблиц в `app/static/style.css`
- [x] 2.3 Проверить в headless Chrome: изменённая ячейка, добавленная и
  удалённая строки, экранированный `\|`, разное число колонок (2 vs 3)

## 3. Финализация

- [x] 3.1 Прогнать `pytest` целиком (74 теста зелёные)
- [x] 3.2 Обновить основную спеку `openspec/specs/diff-viewer/spec.md`
  и README.md

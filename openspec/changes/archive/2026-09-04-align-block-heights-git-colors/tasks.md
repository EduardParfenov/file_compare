# Tasks: align-block-heights-git-colors

## 1. Цветные заглушки и git-стиль фонов (CSS)

- [x] 1.1 В `app/static/style.css` добавить классы `.placeholder-removed` (фон `var(--red-light)`) и `.placeholder-added` (фон `var(--green-light)`), базовый серый `.placeholder` сохранить; проверить, что CSS отдаётся Flask и содержит новые классы
- [x] 1.2 В `app/static/style.css` перевести фоны на git-стиль: `.diff-block.change-added` и `.diff-table tr.change-added` — `var(--green-light)`, `.diff-block.change-changed` — `var(--yellow-light)`; пословную подсветку `seg-*` и хром интерфейса не менять; проверить, что в CSS добавленные блоки/строки используют `--green-light`

## 2. Тип заглушки и выравнивание высот (JS)

- [x] 2.1 В `app/static/app.js` в `renderBlock` добавить параметр типа заглушки: `null`-блок получает класс `placeholder-removed`/`placeholder-added` по `change` противоположной стороны строки в `renderResult`; для табличной группы, где сторона целиком `None`, аналогичный класс в `renderTableSide`; проверить, что страница отдаётся со статусом 200 и `app.js` без синтаксических ошибок (`node --check`)
- [x] 2.2 В `app/static/app.js` добавить проход выравнивания высот после рендера: для каждой пары дочерних элементов `content-left`/`content-right` сбросить `min-height`, измерить `offsetHeight` обеих, установить обеим больший; вызывать после построения панелей в `renderResult` и по событию `resize` окна (debounce не обязателен); проверить `node --check`
- [x] 2.3 Проверить визуально: удалённый многострочный блок имеет напротив красную заглушку той же высоты, добавленный — зелёную, последующие блоки выровнены при синхронной прокрутке; таблица с удалёнными строками не сдвигает контент после неё; изменённая пара разной высоты — подкладка без фона

## 3. Регрессионная проверка

- [x] 3.1 Прогнать `pytest` и убедиться, что все тесты проходят

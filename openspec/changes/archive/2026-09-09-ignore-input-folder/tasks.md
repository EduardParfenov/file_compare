# Задачи: запрет чтения каталога input/

## 1. Обновление контекста проекта

- [x] 1.1 В `openspec/config.yaml` в блоке `context:` (раздел «Конвенции») добавить правило: содержимое каталога `input/` — рабочие заметки, не часть проекта; читать и использовать эти файлы при работе над проектом запрещено. Проверить, что правило появилось в выдаче контекста: `openspec instructions proposal --change ignore-input-folder --json` — поле `context` содержит новое правило
- [x] 1.2 Прогнать `openspec validate ignore-input-folder` — change валиден (skip_specs, ошибок нет)

## 2. Проверка

- [x] 2.1 Прогнать `pytest` — все тесты проходят (изменение только в конфигурации OpenSpec, регрессий быть не должно)

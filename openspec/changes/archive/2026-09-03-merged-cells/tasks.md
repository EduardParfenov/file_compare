# Задачи: корректная конвертация объединённых ячеек

Change оформлен задним числом — все задачи уже выполнены, `pytest` зелёный
(76 тестов).

## 1. Конвертер (spec: markdown-conversion)

- [x] 1.1 Написать тесты `tests/test_docx_converter.py`: таблица с
  горизонтальным merge (текст один раз, повтор пустой, `\n` свёрнут) и
  вертикальным merge (пустая ячейка в строке-продолжении); многострочная
  ячейка не разрывает Markdown-строку
- [x] 1.2 Реализовать в `app/services/docx_converter.py` `_cell_text`
  (сворачивание переносов/пробелов) и дедупликацию merge-ячеек по `tc`
  в `_table_to_markdown`; убедиться, что тесты 1.1 зелёные

## 2. Финализация

- [x] 2.1 Прогнать `pytest` целиком (76 тестов зелёные) и репро-сценарий:
  merge-таблица конвертируется в однострочные строки без дублей
- [x] 2.2 Обновить основную спеку
  `openspec/specs/markdown-conversion/spec.md` и README.md

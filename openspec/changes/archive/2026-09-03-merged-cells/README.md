# merged-cells

Объединённые ячейки таблиц конвертируются корректно (flatten): текст один
раз, повторы пустые, переносы внутри ячейки сворачиваются в пробел.
Оформлено задним числом — реализация уже влита.

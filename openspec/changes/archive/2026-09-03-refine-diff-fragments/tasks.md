# Задачи: уточнение replace-фрагментов diff

Change оформлен задним числом — все задачи уже выполнены, `pytest` зелёный
(58 тестов).

## 1. Алгоритмический diff (spec: document-diff)

- [x] 1.1 Воспроизвести проблему: изменённый + удалённый + добавленный абзацы
  подряд сливаются в один `replace`-фрагмент, всё подсвечивается «изменено»
- [x] 1.2 Написать тесты `tests/test_diffing.py`: уточнение смешанного
  фрагмента (replace/delete/insert), непохожие блоки → delete+insert,
  похожая пара → replace, delete/insert не трогаем, покрытие диапазонов
  без пропусков, нормализация пробелов (NBSP, повторные)
- [x] 1.3 Реализовать `refine_fragments` в `app/services/diffing.py`
  (сопоставление по `ratio`, порог 0.6, ДП-выравнивание, отсев через
  `real_quick_ratio`, защита `MAX_REFINE_PAIRS`) и нормализацию пробельных
  символов в `split_blocks`; убедиться, что тесты 1.2 зелёные

## 2. Пайплайн задач (spec: comparison-jobs)

- [x] 2.1 Применить `refine_fragments` в `run_pipeline` между `find_diffs`
  и классификацией LLM
- [x] 2.2 Переписать `_build_rows`: обход упорядоченных фрагментов с
  заполнением equal-промежутков вместо повторного `SequenceMatcher` и
  сопоставления меток по диапазонам
- [x] 2.3 Написать e2e-тест `tests/test_jobs.py`: изменённый/удалённый/
  добавленный абзацы классифицируются раздельно (changed/removed/added);
  обновить два существующих теста на реалистичные похожие тексты

## 3. Финализация

- [x] 3.1 Прогнать `pytest` целиком (58 тестов зелёные, без сети) и
  проверить репро-сценарий: changed → зелёный, removed → красный,
  added → жёлтый, неизменённые блоки без подсветки
- [x] 3.2 Обновить README.md (п. 2 «Как это работает») и основную спеку
  `openspec/specs/document-diff/spec.md`

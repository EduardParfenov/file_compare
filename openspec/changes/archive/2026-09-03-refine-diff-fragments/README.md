# refine-diff-fragments

Уточнение replace-фрагментов diff по похожести блоков: изменённые блоки
отделяются от удалённых и добавленных; нормализация невидимых пробельных
различий. Оформлено задним числом — реализация уже влита.

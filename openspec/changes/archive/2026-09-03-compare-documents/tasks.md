# Задачи: сравнение двух версий документов

Порядок — TDD: сначала тест (красный), затем реализация (зелёный).
Проверка каждой группы: `pytest` зелёный.

## 1. Подготовка

- [x] 1.1 Добавить `python-docx==1.2.0` в requirements.txt и убедиться, что `pip install -r requirements.txt` проходит без ошибок
- [x] 1.2 Создать пакет `app/services/` (`__init__.py`) и каталог `app/static/`; проверить, что `pytest` по-прежнему зелёный

## 2. Конвертация .docx в Markdown (spec: markdown-conversion)

- [x] 2.1 Написать тесты `tests/test_docx_converter.py` (заголовки/абзацы → Markdown, таблица → Markdown-таблица, пустой документ, повреждённый файл); фикстуры `.docx` генерировать через python-docx в `tmp_path`; убедиться, что тесты падают
- [x] 2.2 Реализовать `app/services/docx_converter.py`; убедиться, что тесты 2.1 зелёные
- [x] 2.3 Написать тест реестра конвертеров (неподдерживаемое расширение → ошибка «формат не поддерживается») и реализовать `app/services/conversion.py` с `convert_to_markdown()`; `pytest` зелёный

## 3. Алгоритмический diff (spec: document-diff)

- [x] 3.1 Написать тесты `tests/test_diffing.py`: разбиение Markdown на блоки (заголовок, абзацы, строки таблиц; пустые строки игнорируются), идентичные документы → пустой diff, `replace`/`delete`/`insert` с позициями блоков; убедиться, что тесты падают
- [x] 3.2 Реализовать `app/services/diffing.py` (`split_blocks`, `find_diffs` на `difflib.SequenceMatcher`); убедиться, что тесты 3.1 зелёные

## 4. Классификация через LLM (spec: llm-classification)

- [x] 4.1 Написать тесты `tests/test_llm.py` с моком chat-модели: корректный JSON → класс + флаг `semantic=true`; невалидный ответ → ровно один ретрай; двойной мусор → fallback по опкоду; исключение соединения → fallback по опкоду и `semantic=false`; убедиться, что тесты падают
- [x] 4.2 Реализовать `app/services/llm.py` (ChatOpenAI из `LLM_BASE_URL`/`LLM_API_KEY`/`LLM_MODEL`, русский системный промпт, извлечение и валидация JSON, ретрай, деградация); убедиться, что тесты 4.1 зелёные без обращений к сети

## 5. Загрузка файлов API (spec: file-upload)

- [x] 5.1 Написать тесты `tests/test_upload.py`: успешная загрузка `.docx` (200, `upload_id`, файл в `UPLOAD_DIR`), отклонение `.txt` (400), отсутствие файла (400), превышение `MAX_CONTENT_LENGTH` (413); убедиться, что тесты падают
- [x] 5.2 Реализовать `app/services/uploads.py` (in-memory реестр, сохранение под `<uuid><ext>`) и эндпоинт `POST /api/upload` в `app/routes.py`; убедиться, что тесты 5.1 зелёные

## 6. Задачи сравнения и пайплайн (spec: comparison-jobs)

- [x] 6.1 Написать тесты `tests/test_jobs.py`: `POST /api/compare` → 202 + `job_id`; неизвестный `upload_id` → 404; без id → 400; `GET /api/jobs/<id>` → статусы/этапы; несуществующая задача → 404; завершённая задача → результат со структурой документов и классами; ошибка конвертации → статус «ошибка»; убедиться, что тесты падают
- [x] 6.2 Реализовать `app/services/jobs.py` (in-memory store, `threading.Thread`, флаг `JOBS_SYNCHRONOUS` для тестов) и пайплайн conversion → diff → llm с обновлением stage-сообщений; эндпоинты `POST /api/compare` и `GET /api/jobs/<job_id>`; убедиться, что тесты 6.1 зелёные

## 7. Frontend: загрузка и статусы (specs: file-upload, comparison-jobs)

- [x] 7.1 Доработать `app/templates/index.html`: зоны «Файл 1»/«Файл 2» с кнопками, кнопка «Начать сравнение» (disabled), контейнер статуса со спиннером, контейнер diff; подключить `app/static/app.js` и `style.css`
- [x] 7.2 Реализовать в `app/static/app.js` выбор файла и drag-and-drop → `POST /api/upload`, отображение имени файла и ошибок валидации, активацию кнопки после двух загрузок; ручная проверка в браузере
- [x] 7.3 Реализовать запуск сравнения и polling `GET /api/jobs/<id>` каждые 3 секунды: спиннер + сообщения «Конвертация файлов...» / «Поиск различий...» / «Анализ через LLM...», скрытие спиннера и вывод ошибки при failed; ручная проверка в браузере

## 8. Frontend: Diff Viewer (spec: diff-viewer)

- [x] 8.1 Реализовать рендер side-by-side панелей с выравниванием блоков и пустыми местами напротив удалённых/добавленных; ручная проверка
- [x] 8.2 Реализовать в `app/static/style.css` палитру (CSS-переменные: зелёные кнопки, жёлтые рамки, серые фоны) и подсветку: изменено — зелёный в обеих панелях, удалено — красный в файле 1, добавлено — жёлтый в файле 2; ручная проверка
- [x] 8.3 Реализовать синхронный скролл панелей и уведомление «LLM недоступна: классификация выполнена алгоритмически» при `semantic=false`; ручная проверка

## 9. Финализация

- [x] 9.1 Прогнать `pytest` целиком (все тесты зелёные, без сетевых обращений) и `flask run` — сквозная проверка сценария: загрузка двух `.docx` → статусы → diff с подсветкой
- [x] 9.2 Обновить README.md (новые эндпоинты, описание процесса) и при необходимости `openspec/project.md`

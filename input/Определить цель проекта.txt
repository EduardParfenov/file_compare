======================================================
Что должно быть
======================================================

1. Определить цель проекта — это ключевое. Что это будет: учебная песочница для экспериментов с AI-агентами, конкретное приложение, курс с примерами?
2. Зафиксировать контекст в openspec/config.yaml — как только выберем стек (TypeScript/Python/...), запишем его туда, чтобы все OpenSpec-артефакты создавались с учётом технологий.
3. Создать первое изменение через OpenSpec — например, add-project-scaffolding (инициализация проекта: стек, сборка, тесты, линт). У меня есть скилл openspec-propose, который сгенерирует полный proposal с дизайном, спеками и задачами.
4. Мелочь: починить .gitignore (убрать его самого из игнора).

Создать файлы/папки: proposal.md, specs/, design.md, tasks.md???

Сходи в инет. посмотри лучшие практики, поспрашивай меня если что не понятно.

Сделай тесты до реализации по TDD.

Не забывай про виртуальное окружение.
Используй Python3.11

Дизайн:
По дизайну позадавай мне вопросы.
Стогий современный стиль.
Сайт стилей: ui.shadcn.com


TDD - о том, что перед тем как реализовывать, вначале напишите тесты. Вписать в файл config.yaml.

1. Сделай в @openspec/config.yaml основные привила прокета - будем делать проект на python, это будет web-интерфейс, для back-end выбираем Flask, все правила в требованиях по стилистики bdd. Где тебе что-то не понятно спроси меня. Описания делай на русском.


======================================================
Промпт (юзера)
======================================================

Я создаю приложение ИИ-агент с веб-интерфейсом. все правила в требованиях по стилистики bdd. Описания делай на русском.
Приложение выполняет функцию сравнения двух файлов. Цель - найти изменения во втором файле относительно первого. Приложение должно уметь прочитать текст и сравнивать файлы форматов doc, docx, xls, xlsx, pdf и сканированные версии pdf. Сканированные PDF требуют оптического распознавания символов (OCR).
Для чтения и сравнения двух файлов должна быть использована LLM. Для передачи файлов в LLM, файлы должны быть спарсены в маркдаун-формат (.md). LLM у нас локальная ее нужно подключать по API OpenAI. LLM размещена на ресурсе http://172.16.48.122:8002/v1, ключ api "4644a0f684518731a781714d34aa6f15f509e197f96582cc022270f78d017e1f", это корпоративный внутренний сервер. Конфигурация должна загружаться из переменных окружения (.env). 

Функционал: Пользователь выбирает два текстовых файла, файлы разных ревизий, исходный файл это первый файл, а второй файл это измененный первый файл. Приложение должно прочитать текст файлов и выполнить их сравнение. Выбор файлов пользователем должен осуществляться  с помощью кнопок "открыть 1 файл" и "открыть 2 файл", а также перетаскиванием файлов из проводника операционной системы в поле приложения.
Результатом должно быть отображение первого и второго файла рядом, различия в них должны быть подсвечены цветом.
Если в первом файле имеется фрагмент, а во втором файле этот фрагмент изменен, этот фрагмент подсвечивается зеленым в обоих файлах. Должна быть подсвечена и связь между первым и вторым документом зеленой линией, где было внесено изменение.
Если в первом файле имеется фрагмент, но во втором файле он удален, этот фрагмент подсвечивается красным в первом файле. подсвечивается связь в каком месте первого файла был удален фрагмент во втором файле.
Если в первом файле нет фрагмента, а во второй файл но добавлен, подсвечивается желтым во втором файле. подсвечивается связь между добавленным фрагментом во втором файле и местом в первом файле куда фрагмент был добавлен.
Должен быть скрол, чтобы листать содержимое документов. Листание должно быть как синхронное, так и каждого документа отдельно, при этом подсвеченные связи должны удлиняться соответственно листанию.
Если что-то не понятно - спроси меня.

Используй Python3.11, LangChain и Flask, не забудь про виртуальное окружение.
Для фронта используй HTML и CSS с gridCSS. Дизайн должен быть в желто-зеленых-серых цветах. Напрмер кнопки - зеленые, рамки -желтые, текст и различные фоны серым разных оттенков. По дизайну задавай мне вопросы. Дизайн должен быть похож на вот такой дизайн: 

Сходи в интернет, посмотри лучшие практики для подобный приложений.
Если что не понятно спроси меня.


======================================================
Промпт Qwen 1
======================================================

/opsx:propose 

Я создаю веб-приложение (отдельная страница) для сравнения двух версий документов с использованием ИИ. Все требования и сценарии должны быть описаны на русском языке в стилистике BDD (Gherkin: Feature, Scenario, Given, When, Then).

### КОНТЕКСТ И ЦЕЛЬ
Приложение сравнивает два файла (исходный и измененный), чтобы найти и семантически оценить изменения. Файлы предварительно конвертируются в Markdown (.md). Сравнение выполняется с помощью LLM через API, совместимый с OpenAI.

### ТЕХНИЧЕСКИЙ СТЕК
- Backend: Python 3.11, Flask, LangChain, python-dotenv (для переменных окружения).
- Frontend: HTML, CSS (Grid CSS), JavaScript (vanilla или минимальный фреймворк). Для линий связи использовать библиотеку типа LeaderLine.js или SVG-оверлей.
- Парсинг: Использовать надежные библиотеки (например, MarkItDown [[1]], unstructured, openpyxl, python-docx). Для сканированных PDF обязательно предусмотреть этап OCR (например, pytesseract или vision-модель).
- Тестирование: TDD. Использовать pytest и mock-объекты для эмуляции ответов LLM, чтобы тесты были детерминированными.

### ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
1. **Загрузка файлов**: 
   - Кнопки "Открыть файл 1" и "Открыть файл 2".
   - Поддержка Drag-and-Drop файлов из проводника в область приложения.
   - Поддерживаемые форматы: doc, docx, xls, xlsx, pdf, сканированные pdf.
2. **Обработка**: 
   - Конвертация обоих файлов в Markdown.
   - Гибридное сравнение: первичный алгоритмический поиск различий + отправка блоков в LLM для семантической классификации (изменено, удалено, добавлено) с целью экономии токенов.
3. **Визуализация (Diff Viewer)**:
   - Отображение двух файлов рядом (side-by-side).
   - Цветовая схема: желто-зелено-серая (кнопки – зеленые, рамки – желтые, текст/фоны – оттенки серого).
   - Подсветка изменений:
     * **Изменено**: фрагмент подсвечивается зеленым в обоих файлах + зеленая линия связи между ними.
     * **Удалено**: фрагмент подсвечивается красным в файле 1 + линия связи, указывающая место удаления в файле 2.
     * **Добавлено**: фрагмент подсвечивается желтым в файле 2 + линия связи, указывающая место вставки относительно файла 1.
   - Скроллинг: синхронный по умолчанию, с возможностью переключения на независимый. Линии связи должны динамически перерисовываться при прокрутке.

### ОГРАНИЧЕНИЯ И БЕЗОПАСНОСТЬ
- API_ENDPOINT и API_KEY должны строго читаться из переменных окружения (.env). Не хардкодить их в коде!
- (Уточнение для ИИ): Адрес LLM: http://172.16.48.122:8002/v1 (внутренний корпоративный сервер), ключ "4644a0f684518731a781714d34aa6f15f509e197f96582cc022270f78d017e1f".

### ДИЗАЙН
Дизайн должен быть похож на: [https://ui.shadcn.com/create?preset=b1FjSQKCu]

### ЗАДАЧА ДЛЯ ИИ (ШАГИ)
1. Задай мне уточняющие вопросы, если какие-то требования противоречивы или неполны (особенно про дизайн).
2. Сгенерируй BDD-сценарии (Gherkin) для основных пользовательских путей.
3. Предложи архитектуру проекта и список зависимостей (requirements.txt).
4. Напиши тесты (TDD) с моками для LLM перед реализацией основного кода.
5. Реализуй backend и frontend поэтапно.

Если что-то непонятно, спроси меня перед началом генерации кода.

======================================================
Промпт Qwen 2
======================================================

/opsx:propose 

Я создаю веб-приложение (отдельная веб-страница) для ИИ-агента, который сравнивает две версии документов. Все требования, сценарии и описания должны быть на русском языке в стилистике BDD (Gherkin: Feature, Scenario, Given, When, Then).

### КОНТЕКСТ И ЦЕЛЬ
Приложение сравнивает два файла (исходный и измененный), чтобы найти и семантически оценить изменения. Файлы предварительно конвертируются в Markdown (.md). Сравнение выполняется с помощью LLM через API, совместимый с OpenAI.

### ТЕХНИЧЕСКИЙ СТЕК
- Backend: Python 3.11, Flask, LangChain, python-dotenv.
- Deployment: Целевая платформа — Linux Ubuntu. Для запуска использовать WSGI-сервер (например, Gunicorn), управляемый через systemd или supervisor. (Строго без IIS или Windows-специфичных решений).
- Frontend: HTML, CSS (Grid CSS), JavaScript (vanilla). Для визуализации линий связи между блоками использовать библиотеку типа LeaderLine.js или SVG-оверлей, пересчитывающий координаты при скролле.
- Парсинг: Использовать надежные кроссплатформенные библиотеки (например, MarkItDown, unstructured, openpyxl, python-docx). Для сканированных PDF обязательно предусмотреть этап OCR (например, pytesseract или vision-модель LLM).
- Тестирование: Строгий TDD. Использовать pytest и mock-объекты для эмуляции ответов LLM, чтобы тесты парсинга и логики были детерминированными и быстрыми.

### ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
1. **Загрузка файлов**: 
   - Кнопки "Открыть файл 1" и "Открыть файл 2".
   - Поддержка Drag-and-Drop файлов из проводника ОС в область приложения.
   - Поддерживаемые форматы: doc, docx, xls, xlsx, pdf, сканированные pdf.
2. **Обработка**: 
   - Конвертация обоих файлов в Markdown.
   - Гибридное сравнение: первичный алгоритмический поиск различий (например, через difflib) + отправка подозрительных блоков в LLM для семантической классификации (изменено, удалено, добавлено) с целью экономии токенов и контекста.
3. **Визуализация (Diff Viewer)**:
   - Отображение двух файлов рядом (side-by-side).
   - Цветовая схема: желто-зелено-серая (кнопки – зеленые, рамки – желтые, текст и фоны – оттенки серого).
   - Подсветка изменений:
     * **Изменено**: фрагмент подсвечивается зеленым в обоих файлах + зеленая линия связи между ними.
     * **Удалено**: фрагмент подсвечивается красным в файле 1 + линия связи, указывающая место удаления в файле 2.
     * **Добавлено**: фрагмент подсвечивается желтым в файле 2 + линия связи, указывающая место вставки относительно файла 1.
   - Скроллинг: синхронный по умолчанию, с возможностью переключения на независимый. Линии связи должны динамически перерисовываться при прокрутке.

### ОГРАНИЧЕНИЯ И БЕЗОПАСНОСТЬ
- API_ENDPOINT и API_KEY должны строго читаться из переменных окружения (.env). Запрещено хардкодить их в коде.
- Адрес LLM: http://172.16.48.122:8002/v1 (внутренний корпоративный сервер). Ключ 4644a0f684518731a781714d34aa6f15f509e197f96582cc022270f78d017e1f

### ДИЗАЙН
Дизайн должен быть похож на: [https://ui.shadcn.com/create?preset=b1FjSQKCu].

### ЗАДАЧА ДЛЯ ИИ (ШАГИ)
1. Задай мне уточняющие вопросы, если какие-то требования противоречивы или неполны (особенно про дизайн, детали OCR и зависимости для Ubuntu, такие как tesseract-ocr).
2. Сгенерируй BDD-сценарии (Gherkin) для основных пользовательских путей.
3. Предложи архитектуру проекта и список зависимостей (requirements.txt + системные пакеты Ubuntu, если нужны).
4. Напиши тесты (TDD) с моками для LLM перед реализацией основного кода.
5. Реализуй backend и frontend поэтапно.

Если что-то непонятно, спроси меня перед началом генерации кода.


======================================================
Промпт Qwen 3
======================================================

/opsx:propose 

Я создаю веб-приложение (отдельная веб-страница) для ИИ-агента, который сравнивает две версии документов. Все требования и сценарии на русском языке в стилистике BDD (Gherkin: Feature, Scenario, Given, When, Then).

### КОНТЕКСТ И ЦЕЛЬ
Пользователь загружает два файла (исходный и измененный). Приложение конвертирует их в Markdown, выполняет алгоритмический поиск различий, а затем использует LLM для семантической классификации каждого изменения (изменено / удалено / добавлено). Результат отображается в виде side-by-side diff с цветовой подсветкой и линиями связи между связанными блоками.

### ТЕХНИЧЕСКИЙ СТЕК
- Backend: Python 3.11, Flask, LangChain, python-dotenv.
- Deployment: Linux Ubuntu, WSGI-сервер Gunicorn, управление через systemd.
- Frontend: HTML, CSS (Grid CSS), JavaScript (vanilla). Для линий связи использовать LeaderLine.js или SVG-оверлей с перерисовкой при скролле.
- Парсинг:
  * `.docx` → Markdown через `python-docx` (или `MarkItDown`).
  * `.xlsx` → Markdown через `openpyxl`. Таблицы конвертировать в Markdown-таблицы. Если таблица слишком большая (более 50 строк), разбивать на части или сворачивать.
- Сравнение: **гибридный подход**:
  1. Алгоритмический diff через `difflib` для поиска различающихся блоков.
  2. Каждый блок отправляется в LLM для семантической классификации: "изменено" (смысл тот же, но текст перефразирован), "удалено", "добавлено".
  3. Это экономит токены и повышает точность.
- Тестирование: TDD через pytest. **Обязательно использовать mock-объекты для LLM**, чтобы тесты были детерминированными.

### ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
1. **Загрузка файлов**: 
   - Две кнопки: "Открыть файл 1" и "Открыть файл 2".
   - Drag-and-Drop: пользователь может перетащить файлы из проводника в соответствующие зоны.
   - Поддерживаемые форматы: `.docx`, `.xlsx`.
   - Валидация: если формат не поддерживается — показать ошибку.
2. **Обработка**:
   - Конвертация обоих файлов в Markdown.
   - Алгоритмический поиск различий.
   - Отправка блоков в LLM для семантической классификации.
3. **Визуализация (Diff Viewer)**:
   - Отображение двух файлов рядом (side-by-side).
   - Цветовая схема: желто-зелено-серая (кнопки – зеленые, рамки – желтые, текст и фоны – оттенки серого).
   - Подсветка различий:
     * **Изменено**: зеленый фон в обоих файлах + зеленая линия связи.
     * **Удалено**: красный фон в файле 1 + линия связи, указывающая место удаления в файле 2.
     * **Добавлено**: желтый фон в файле 2 + линия связи, указывающая место вставки в файле 1.
   - Синхронный скролл по умолчанию, с возможностью переключения на независимый.
   - Линии связи динамически перерисовываются при прокрутке.

### ОГРАНИЧЕНИЯ И БЕЗОПАСНОСТЬ
- API_ENDPOINT и API_KEY должны строго читаться из переменных окружения (.env). Запрещено хардкодить их в коде.
- Адрес LLM: http://172.16.48.122:8002/v1 (внутренний корпоративный сервер, совместим с OpenAI API).
- Обработка больших файлов: если документ превышает лимит контекста LLM, разбивать на части и обрабатывать постранично/поблочно.

### ДИЗАЙН
Дизайн должен быть похож на: [https://ui.shadcn.com/create?preset=b1FjSQKCu].

### ЗАДАЧА ДЛЯ ИИ (ШАГИ)
1. Задай уточняющие вопросы, если что-то непонятно (особенно про дизайн, обработку больших Excel-таблиц и стратегию разбиения документов для LLM).
2. Сгенерируй BDD-сценарии (Gherkin) для основных путей.
3. Предложи архитектуру проекта, requirements.txt и системные пакеты Ubuntu (если нужны).
4. Напиши тесты (TDD) с моками для LLM перед реализацией.
5. Реализуй backend и frontend поэтапно.

Если что-то непонятно — спроси меня перед началом генерации кода.


======================================================
Промпт Qwen 4
======================================================

/opsx:propose 

Я создаю веб-приложение (отдельная веб-страница) для ИИ-агента, который сравнивает две версии документов. Все требования и сценарии на русском языке в стилистике BDD (Gherkin: Feature, Scenario, Given, When, Then).

### КОНТЕКСТ И ЦЕЛЬ
Пользователь загружает два файла (исходный и измененный). Приложение конвертирует их в Markdown, выполняет алгоритмический поиск различий, а затем использует LLM для семантической классификации каждого изменения (изменено / удалено / добавлено). Результат отображается в виде side-by-side diff с цветовой подсветкой.

### ТЕХНИЧЕСКИЙ СТЕК
- Backend: Python 3.11, Flask, LangChain, python-dotenv.
- Deployment: Linux Ubuntu, WSGI-сервер Gunicorn, управление через systemd.
- Frontend: Чистые HTML, CSS, JavaScript (vanilla). БЕЗ React, Tailwind, npm-сборщиков, БЕЗ shadcn/ui.
- Парсинг:
  * `.docx` → Markdown через `python-docx`.
  * `.xlsx` → Markdown через `openpyxl`. Таблицы конвертировать в Markdown-таблицы.
- Сравнение: **гибридный подход**:
  1. Алгоритмический diff через `difflib` для поиска различающихся блоков.
  2. Каждый блок отправляется в локальную LLM для семантической классификации: "изменено", "удалено", "добавлено".
  3. Лимиты контекста LLM не учитываем (локальная модель).
- Тестирование: TDD через pytest. **Обязательно использовать mock-объекты для LLM**, чтобы тесты были детерминированными.
- Управление зависимостями: использовать `uv` и виртуальное окружение `.venv`.

### ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
1. **Загрузка файлов**: 
   - Две кнопки: "Открыть файл 1" и "Открыть файл 2".
   - Drag-and-Drop: пользователь может перетащить файлы из проводника в соответствующие зоны.
   - Поддерживаемые форматы: `.docx`, `.xlsx`.
   - Валидация: если формат не поддерживается — показать ошибку.
2. **Обработка**:
   - Конвертация обоих файлов в Markdown.
   - Алгоритмический поиск различий.
   - Отправка блоков в LLM для семантической классификации.
3. **Визуализация (Diff Viewer)**:
   - Отображение двух файлов рядом (side-by-side).
   - Цветовая схема: желто-зелено-серая (кнопки – зеленые, рамки – желтые, текст и фоны – оттенки серого).
   - Подсветка различий:
     * **Изменено**: зеленый фон в обоих файлах.
     * **Удалено**: красный фон в файле 1.
     * **Добавлено**: желтый фон в файле 2.
   - **Только синхронный скролл**.

### ОГРАНИЧЕНИЯ И БЕЗОПАСНОСТЬ
- API_ENDPOINT и API_KEY должны строго читаться из переменных окружения (.env). Запрещено хардкодить их в коде.
- Адрес LLM: http://172.16.48.122:8002/v1 (внутренний корпоративный сервер, совместим с OpenAI API).

### ДИЗАЙН
Минималистичный дизайн на чистом CSS. Цветовая палитра: желто-зелено-серая. Кнопки зелёные, рамки желтые, текст и фоны различных оттенков серого.

### ЗАДАЧА ДЛЯ ИИ (ШАГИ)
1. Задай уточняющие вопросы, если что-то непонятно (особенно про обработку больших Excel-таблиц и промпт для LLM).
2. Сгенерируй BDD-сценарии (Gherkin) для основных путей.
3. Предложи архитектуру проекта, requirements.txt и системные пакеты Ubuntu (если нужны).
4. Напиши тесты (TDD) с моками для LLM перед реализацией.
5. Реализуй backend и frontend поэтапно.

Если что-то непонятно — спроси меня перед началом генерации кода.


======================================================
Промпт Qwen 5
======================================================

/opsx:propose 

Я создаю веб-приложение (отдельная веб-страница) для ИИ-агента, который сравнивает две версии документов. Все требования и сценарии на русском языке в стилистике BDD (Gherkin: Feature, Scenario, Given, When, Then).

### КОНТЕКСТ И ЦЕЛЬ
Пользователь загружает два файла (исходный и измененный). Приложение конвертирует их в Markdown, выполняет алгоритмический поиск различий, а затем использует LLM для семантической классификации каждого изменения (изменено / удалено / добавлено). Результат отображается в виде side-by-side diff с цветовой подсветкой.

### ТЕХНИЧЕСКИЙ СТЕК
- Backend: Python 3.11, Flask, LangChain, python-dotenv.
- Виртуальное окружение: использовать `python -m venv .venv`.
- Запуск: стандартный `flask run` для разработки. Архитектура должна быть готова к упаковке в Docker в будущем: конфигурация через переменные окружения, относительные пути.
- Frontend: Чистые HTML, CSS, JavaScript (vanilla).
- Парсинг:
  * `.docx` → Markdown через `python-docx`.
  * `.xlsx` → Markdown через `openpyxl`. Таблицы конвертировать в Markdown-таблицы.
- Сравнение: **гибридный подход**:
  1. Алгоритмический diff через `difflib` для поиска различающихся блоков.
  2. Каждый блок отправляется в локальную LLM для семантической классификации: "изменено", "удалено", "добавлено".
- Тестирование: TDD через pytest. Использовать mock-объекты для LLM, чтобы тесты были детерминированными.

### ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
1. **Загрузка файлов**: 
   - Две кнопки: "Открыть файл 1" и "Открыть файл 2".
   - Третья кнопка: **"Начать сравнение"** — запускает процесс сравнения после загрузки обоих файлов.
   - Drag-and-Drop: пользователь может перетащить файлы из проводника в соответствующие зоны.
   - Поддерживаемые форматы: `.docx`, `.xlsx`.
   - Валидация: если формат не поддерживается — показать ошибку.
   - Кнопка "Начать сравнение" активна только после загрузки обоих файлов.
2. **Обработка**:
   - Конвертация обоих файлов в Markdown.
   - Алгоритмический поиск различий.
   - Отправка блоков в LLM для семантической классификации.
3. **Визуализация процесса обработки**:
   - Во время обработки показывать индикатор загрузки (спиннер) и статусное сообщение.
   - Статусные сообщения по этапам:
     * "Конвертация файлов..."
     * "Поиск различий..."
     * "Анализ через LLM..."
   - Это нужно, чтобы пользователь видел, что приложение работает, а не зависло.
4. **Визуализация результата (Diff Viewer)**:
   - Отображение двух файлов рядом (side-by-side).
   - Цветовая схема: желто-зелено-серая (кнопки – зеленые, рамки – желтые, текст и фоны – оттенки серого).
   - Подсветка различий:
     * **Изменено**: зеленый фон в обоих файлах.
     * **Удалено**: красный фон в файле 1.
     * **Добавлено**: желтый фон в файле 2.
   - Синхронный скролл обеих панелей.

### ОГРАНИЧЕНИЯ И БЕЗОПАСНОСТЬ
- Конфигурация только через переменные окружения (.env).
- Адрес LLM: http://172.16.48.122:8002/v1 (внутренний корпоративный сервер, совместим с OpenAI API).

### ДИЗАЙН
Минималистичный дизайн на чистом CSS. Цветовая палитра: желто-зелено-серая. Кнопки зелёные, рамки желтые, текст и фоны различных оттенков серого.

### ЗАДАЧА ДЛЯ ИИ (ШАГИ)
1. Задай уточняющие вопросы, если что-то непонятно (особенно про обработку больших Excel-таблиц и промпт для LLM).
2. Сгенерируй BDD-сценарии (Gherkin) для основных путей.
3. Предложи архитектуру проекта и requirements.txt.
4. Напиши тесты (TDD) с моками для LLM перед реализацией.
5. Реализуй backend и frontend поэтапно.

Если что-то непонятно — спроси меня перед началом генерации кода.
